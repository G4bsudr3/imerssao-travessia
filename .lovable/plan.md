## Remover "90 min" da agenda

### O que mudar
- `src/slides/slideManifest.tsx` linha 77: alterar o `eyebrow` do slide `agenda` de `"AGENDA · 90 MIN"` para `"AGENDA"`.
- `src/slides/slideManifest.tsx` linha 2: atualizar o comentário de cabeçalho de `"4 atos · ~90 min"` para `"4 atos"`.

### Nada mais será alterado. A estrutura dos 4 atos e os demais slides permanecem intactos.